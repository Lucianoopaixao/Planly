{
  "name": "@planly/user-service",
  "version": "1.0.0",
  "description": "Serviço de usuários — autenticação, cadastro e horários fixos",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "vitest": "^2.1.4",
    "supertest": "^7.0.0"
  }
}
